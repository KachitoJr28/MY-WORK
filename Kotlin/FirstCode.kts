fun main() {
    println("I am Omefome")
    val fname="Omefome A.O" //this can't be changed since it's val
    var age= 12 //this can be changed since it's var
    /*
     * data types in kotlin are int, double, char, boolean and string
     * you can choose not to specify the data type of a variable as done above
     * or you can specify it
     */
    val myNum: Int = 5
    val myDouble: Double = 3.14
    val myChar: Char = 'o'
    val myBoolean: Boolean = true
    val myString: String = "Yes"
}